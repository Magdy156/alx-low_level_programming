#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: first to be multiplied
 * @b: second  to be multiplied
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
